'a: (#self(), var_name) => {} // pattern declaration

'a() // pattern call

(#lhs()) => {} // standalone function

A -> B: {

}

let a: 4
a: (b: 3) // technically not a declaration

A: struct {
  let x
}
